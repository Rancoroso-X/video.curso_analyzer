# ======================================================================
# 🎓 ANALISADOR DE CURSOS NASCO v4.0 ULTIMATE - CONFIGURAÇÃO
# ======================================================================
# Copie este arquivo para .env e configure suas credenciais

# --- CONFIGURAÇÃO DA OPENAI (OBRIGATÓRIO) ---
# Obtenha sua chave em: https://platform.openai.com/api-keys
OPENAI_API_KEY=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

# Organização OpenAI (opcional - só se você tem uma)
# OPENAI_ORG_ID=org-XXXXXXXXXXXXXXXXXXXXXXXX

# --- CONFIGURAÇÕES DE IA (OPCIONAL) ---
# Modelo padrão (gpt-3.5-turbo, gpt-4, gpt-4o)
DEFAULT_GPT_MODEL=gpt-3.5-turbo

# Temperatura padrão (0.0 a 1.0)
DEFAULT_TEMPERATURE=0.3

# Tokens máximos padrão
DEFAULT_MAX_TOKENS_SUMMARY=400
DEFAULT_MAX_TOKENS_INSIGHTS=600
DEFAULT_MAX_TOKENS_QUIZ=700

# --- CONFIGURAÇÕES DE WHISPER ---
# Modelo Whisper padrão (tiny, base, small, medium, large)
DEFAULT_WHISPER_MODEL=small

# Idioma padrão para transcrição (pt, en, es, etc.)
DEFAULT_WHISPER_LANGUAGE=pt

# --- CONFIGURAÇÕES DE SISTEMA ---
# Pasta padrão para cursos (opcional)
# DEFAULT_COURSES_PATH=/caminho/para/seus/cursos

# Habilitar cache (true/false)
ENABLE_CACHE=true

# Tamanho máximo de arquivo para processamento (em MB)
MAX_FILE_SIZE_MB=500

# --- CONFIGURAÇÕES DE PERFORMANCE ---
# Número máximo de threads para processamento paralelo
MAX_THREADS=4

# Timeout para requisições de IA (em segundos)
AI_REQUEST_TIMEOUT=120

# --- CONFIGURAÇÕES DE INTERFACE ---
# Tema padrão (light, dark, auto)
DEFAULT_THEME=auto

# Habilitar modo debug (true/false)
DEBUG_MODE=false

# --- CONFIGURAÇÕES DE BACKUP ---
# Pasta para backups automáticos (opcional)
# BACKUP_PATH=/caminho/para/backups

# Intervalo de backup em horas (0 = desabilitado)
BACKUP_INTERVAL_HOURS=24

# --- CONFIGURAÇÕES AVANÇADAS ---
# Habilitar processamento de imagens em PDFs (true/false)
ENABLE_PDF_IMAGES=true

# Habilitar análise de sentimentos (true/false)
ENABLE_SENTIMENT_ANALYSIS=false

# Habilitar detecção automática de idioma (true/false)
ENABLE_LANGUAGE_DETECTION=true

# --- LOGS E MONITORAMENTO ---
# Nível de log (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Pasta para logs (opcional)
# LOG_PATH=/caminho/para/logs

# Habilitar telemetria anônima (true/false)
ENABLE_TELEMETRY=false

# ======================================================================
# INSTRUÇÕES:
# 
# 1. Copie este arquivo para .env:
#    cp .env.template .env
#
# 2. Configure pelo menos a OPENAI_API_KEY
#
# 3. Ajuste outras configurações conforme necessário
#
# 4. NUNCA comite o arquivo .env (já está no .gitignore)
# NOIZ Q VOA
# ======================================================================